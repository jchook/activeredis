#!/bin/bash
DIR=$(dirname $(dirname "$(readlink -f "$0")"))
CMD=${1:-"$DIR/vendor/bin/phpunit --bootstrap $DIR/etc/phpunit.bootstrap.php $DIR/spec"}
find "$DIR/src" "$DIR/spec" -name '*.php' | entr $CMD | awk -f "$DIR/etc/test-colorize.awk"
